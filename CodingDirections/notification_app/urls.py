from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

# urlpatterns = [
#         path(r'celery',views.celery,name='celery'), #for testing celery
# ]